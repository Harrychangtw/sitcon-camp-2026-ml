# Slide 24 — The Attention Tracker
- **Archetype**: capsule-list
- **Grid & layout**: Two-zone split (slide 24). Left ~40%: a large section title vertically centered, left-aligned, sitting in the left third/left-half. Right ~50–55%: a single column of four stacked capsule cards, evenly spaced with consistent vertical gutters, top-to-bottom filling the safe area. Right column is right-aligned to a margin. Comfortable outer margins.
- **Type roles**: head / body. Title "方法優勢" is the largest element — bold white CJK, ~2–3× the capsule heading size. Capsule headings are bold white CJK; capsule body text is smaller, lighter grey CJK running 1–2 lines. No mono. Bodies contain occasional inline Latin ("LLM", "Heads", "1.5B", "100%") set inline with CJK, standard mixed-script spacing.
- **Color / tokens**: Near-black background; capsules are a slightly lighter dark-grey rounded panel tier (raised card). Foreground split: white headings vs grey body. Accent color on this slide comes almost entirely from the emoji glyphs — lime is essentially absent (no lime rules, dots, or highlights). No viridis.
- **Components present**: Four CAPSULE cards, vertical stack (single column of 4). Anatomy per capsule: emoji icon (top-left of card) + bold white CJK heading beside/below it on the left; a thin vertical divider rule separates the left (icon+heading) zone from the right; single body block (1–2 grey lines) on the right. **No English subtitle** under the headings (contrast with slide 27, which adds one). Internal padding is generous and uniform. The four: 💡 無需訓練 — "不需要收集幾萬筆資料微調，只需一些生成資料即可找出 Heads。"; ⚡ 零耗算力 — "Focus Score 是在 LLM 推論時「順便」讀出來的，幾乎不增加延遲。"; 💪 降維打擊 — "攻擊者可以改變語言、排版，但極難在不引發分心下控制模型。"; 🚀 小巧強悍 — "實測證明，即便是 1.5B 參數的小模型，也能展現近 100% 的攔截率。"
- **Motion hints**: Even four-card stack suggests a sequential top-to-bottom reveal (one capsule fades/slides in at a time), though nothing in the still mandates it.
- **Reusable pattern**: This slide is an instance of "big left-aligned section title + right-hand vertical stack of icon/heading/divider/body capsules" (shared with slide 27).
- **One-offs / inconsistencies**: Capsule internal layout uses a vertical divider between the icon+heading block and the body — consistent with slide 27, so this reads as canonical. The absence of an English subtitle here (vs slide 27's bilingual headings) is the key rule-vs-variant signal: two capsule variants exist (zh-only here, zh+en on 27).
