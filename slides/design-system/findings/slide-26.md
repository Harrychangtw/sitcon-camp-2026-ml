# Slide 26 — Exp 1: Context Length
> Footer-right label read as "Exp 1: Context Length" — best reading; it is partially occluded by the datamosh glitch overlay in the bottom-right corner (confirmed against the clean crops on slides 27–28, which are identical).

- **Archetype**: section-divider
- **Grid & layout**: Left-third title block over a full-bleed decorative background (slide 26). Two-line heading sits at left, vertically centered / slightly below center. The right two-thirds are given entirely to the glitch imagery. Text respects a left safe-margin; otherwise the background is full-bleed to all edges.
- **Type roles**: head only (plus footer). Kicker line "Section 03." in grey Latin (medium size), and below it the CJK section title "這個防禦真的完美嗎?" in white, bold, larger. This is a clear white-vs-grey split: grey Latin kicker over white CJK title. Note the question mark here is a halfwidth "?" (unlike the fullwidth "？" on slide 25) — a mixed-script punctuation quirk. No mono, no body.
- **Color / tokens**: Background is a datamosh / RGB pixel-smear glitch field — horizontal streaking with chromatic-aberration bands of magenta, cyan, green, and violet over dark. This is the darkest-tier base with a decorative full-bleed image on top rather than a flat token color. Foreground: grey kicker + white title. No lime accent (the color energy comes from the glitch artwork, not the lime token). No structured viridis ramp — the multicolor is glitch noise, not a data ramp.
- **Components present**: (1) Section kicker "Section 03." (grey Latin). (2) Section title (white CJK). (3) Full-bleed glitch/datamosh background image. No capsules, charts, or dividers.
- **Motion hints**: The glitch texture strongly implies motion/animation (an animated datamosh or video loop behind static text); the "Section 0X." kicker implies a repeatable section-divider template across the deck.
- **Reusable pattern**: This slide is an instance of "section divider = grey 'Section 0X.' kicker + white CJK section question, left-aligned over a full-bleed glitch background."
- **One-offs / inconsistencies**: The datamosh background is a section-divider signature (distinct from the flat near-black used on content slides). Halfwidth "?" vs slide 25's fullwidth "？" is an inconsistency in punctuation width. Footer legibility is compromised by the glitch — an accidental side-effect of the full-bleed treatment, not an intended style.
