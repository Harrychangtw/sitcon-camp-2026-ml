# Slide 08 — The Injection Threat
- **Archetype**: flow-diagram
- **Grid & layout**: Three-stage horizontal flow across a full-bleed dark canvas (slide 8). Three grey column headers pin the top: 進入點 (left), 漏洞成因 (center), 最終衝擊 (right), each sitting above its stage and separated by faint vertical hairline rules that run the full height. Left column holds a vertical stack of 2 capsules; center column holds a vertical stack of 3 capsules; right column holds a single tall text block. Connector lines/elbows run left→center→right implying causal flow. Content occupies a centered safe area with comfortable outer margins; footer sits below the baseline rules.
- **Type roles**: head (capsule headings, white, bold), body (capsule body lines, lighter grey, smaller), and stage labels (grey, small, tracked, sit as section headers). Mixed CJK+Latin throughout: left-column headings are Latin-only ("Direct Injection" / "Indirect Injection"), center headings are zh-only (語境混淆 / 過度服從 / 權威信任), right block is zh with embedded Latin ("XML"/"Agent"). Body copy is CJK with inline Latin (RLHF, XML, API). Headings ~1.5–2× body, heavier weight.
- **Color / tokens**: Near-black background tier. Foreground split: grey stage headers (dimmer tier) vs white capsule headings (bright tier) vs mid-grey body — a clear title-vs-supporting split. Capsule surfaces are a slightly raised dark grey panel with rounded corners. No lime accent visible on this slide. No viridis/heatmap.
- **Components present**:
  - Stage headers (3): grey small-caps-like labels 進入點 / 漏洞成因 / 最終衝擊.
  - Capsules, LEFT stack (2, vertical): each = small red/round emoji icon (top-right of heading; appearance unsure — looks like a target/dartboard and a magnifier, marked unsure) + **Latin-only heading** (Direct Injection / Indirect Injection) + single zh body line. No zh heading, no en subtitle. Rounded dark panel, internal padding generous.
  - Capsules, CENTER stack (3, vertical): each = small emoji icon at right of heading (blue droplet-like / yellow hand / bank/building — unsure) + **zh-only heading** (語境混淆 / 過度服從 / 權威信任) + single zh body line. No en subtitle.
  - Right block (1): a red document/emoji icon + zh heading 災難擴散 + a two-paragraph zh body (longer than a capsule body line). Reads more like a terminal statement panel than a capsule.
  - Connector elbow lines linking the stages.
- **Motion hints**: The three-stage left-to-right causal structure invites a staged reveal (entry → cause → impact), but nothing in the still confirms animation.
- **Reusable pattern**: This slide is an instance of a three-stage cause-and-effect flow built from stacked capsules under grey stage headers.
- **One-offs / inconsistencies**: The capsule heading treatment is INCONSISTENT within the slide — left capsules are Latin-only, center capsules are zh-only, and neither carries the zh+en двух-line heading seen on slide 9. This three-way variance (vs slide 9's canonical zh+en form) is the key rule-vs-accident signal. The right "災難擴散" block breaks capsule anatomy (multi-paragraph body, no single-line rule) and is better read as a statement panel. Icons are ambiguous — described by appearance, codepoints unsure.
