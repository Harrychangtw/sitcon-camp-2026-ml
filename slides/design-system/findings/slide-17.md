# Slide 17 — The Attention Tracker
- **Archetype**: statement (a two-line rhetorical CJK question with a lime-highlighted key phrase).
- **Grid & layout**: Full-bleed dark canvas, faint square grid (slide 17). Two lines of CJK text sit around the vertical middle, left-aligned to roughly the horizontal center (the text block starts ~1/4 from the left and does not span full width) (slide 17). Large surrounding whitespace top and bottom; no columns, no other elements besides the footer.
- **Type roles**: body/statement only — one large CJK type role across two lines; no separate head, no mono (slide 17). Line 1 ends with a Latin ellipsis "..."; line 2 carries the emphasized phrase. Weight is bold/heavy throughout. Pure CJK with full-width brackets 「」 and a trailing full-width-ish "?" (slide 17).
- **Color / tokens**: Background darkest tier (slide 17). Foreground = white tier for the bulk of the sentence; the key phrase 「分心的腦袋」 is set in the lime accent (the deck's signature yellow-green) to spotlight it (slide 17). No heatmap/viridis here. This is the canonical "white body + single lime emphasis run" pattern.
- **Components present**: Single statement text block only (two lines). No capsules, no diagram, no container (slide 17).
- **Motion hints**: The line-1 "..." then line-2 question implies a two-step reveal (setup, then the punch question) (slide 17) — inferred.
- **Reusable pattern**: "Full-screen CJK rhetorical statement with one lime-highlighted phrase" — the emphasis-statement instance.
- **One-offs / inconsistencies**: None notable; this is a clean canonical statement slide. Text block is left-anchored near center rather than truly centered, consistent with the deck's left-optical alignment habit.
