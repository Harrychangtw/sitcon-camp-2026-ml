# Slide 15 — The Attention Tracker
- **Archetype**: other(annotated-sentence / labeled-callout diagram) — a single centered CJK sentence with head-attribution callouts pointing at spans; closest enum neighbor is flow-diagram but the "flow" is annotation arrows onto text, not boxes.
- **Grid & layout**: Full-bleed dark canvas with a faint square grid (slide 15). Oversized Latin section title "Multi-head Attention." spans the top, left-aligned, bleeding to both edges (slide 15). One horizontally centered CJK sentence sits on the vertical midline (slide 15). Two callout labels sit BELOW the sentence (left-of-center and right-of-center) and one callout sits ABOVE-right; arrows connect each label to an underlined span in the sentence (slide 15). Generous whitespace; no columns.
- **Type roles**: head = huge low-contrast grey Latin title (slide 15); body = large white CJK sentence, the largest readable running text (slide 15); labels = small bold white CJK terms each prefixed by an emoji (slide 15). Title is far larger than body (roughly title ≈ 2× the CJK line height), and heavier in weight but rendered in dark grey so it reads as a watermark. CJK and Latin mix only in the title band vs. body; within the sentence it is pure CJK with full-width punctuation (slide 15).
- **Color / tokens**: Background = darkest tier (near-black) with a subtle grid (slide 15). Foreground title = grey tier (low contrast, watermark-like); body sentence = white tier (high contrast) (slide 15). Accent usage is multi-hued here, NOT the usual single lime: each of the four attention "heads" gets its own accent — magenta/pink, lime/yellow-green, purple, plus the pink underline (slide 15). The colored underline dashes beneath spans and the matching arrow colors encode which head attends to which span (slide 15).
- **Components present**:
  - Oversized grey watermark section title (slide 15).
  - Centered CJK statement line with two colored dashed underlines marking attended spans (slide 15).
  - Four head-callout labels = emoji + bold CJK name (🤔 情境分析師, 👮 規則守護者, 🤓 文法糾察隊, and the pink-linked one), each tied by a colored arrow to a span (slide 15). These are labels, NOT capsules — no divider, no body line, no container.
- **Motion hints**: The four color-coded arrows/labels suggest a staged reveal where heads light up one at a time onto the sentence (slide 15) — inferred, not certain.
- **Reusable pattern**: "A single CJK sentence as the stage, with color-coded head callouts arrowing onto specific tokens" — the multi-head-attention annotation instance.
- **One-offs / inconsistencies**: Multi-color accent scheme (magenta/lime/purple) is unusual vs. the deck's typical single-lime accent; here color is semantic (one hue per head) (slide 15). Emoji used as inline label icons is specific to this callout treatment.
