# Slide 33 — Exp 3: Forced Prefixes
- **Archetype**: data-viz
- **Grid & layout**: Left ~35% column: a two-line title upper-left ("跨語攻擊：" / "能騙過聚光燈嗎？") and, below it, a "Key Insight:" heading with a numbered 1/2 analysis list. Right ~60%: a large square heatmap with a vertical colorbar to its right. Y-axis = "Injection Language", X-axis = "Command Language", both labeled with the 10 languages. Footer present (slide 33).
- **Type roles**: head (bold CJK two-line title; bold zh lead phrases in the Key Insight list "模型規模差異：", "語系的親近："), body (CJK running text in list items), mono/label (Latin heatmap title "Qwen3-0.6B Cross-Lingual Focus Score Heatmap", axis language labels English/German/French/…/Bengali, colorbar numeric ticks). Colorbar carries CJK end-labels "高分 攻擊成功率" (top) and "低分 攻擊失敗率" (bottom).
- **Color / tokens**: Near-black background. Title white-vs-grey split: "跨語攻擊：" mid-grey, "能騙過聚光燈嗎？" bright white bold. Heatmap uses a VIRIDIS ramp (dark blue/teal → green → yellow) mapped to the vertical colorbar; colorbar 高分/yellow at top, 低分/dark at bottom, semantically labeled 攻擊成功率 → 攻擊失敗率. Inside the Key Insight body, a few phrases are highlighted (e.g. "同語系" / "語系差異大") in a lighter/accent tone versus the grey body. No dashed annotation boxes on this slide.
- **Components present**: (1) Two-line title. (2) "Key Insight:" numbered list (2 items). (3) Viridis heatmap (10×10) with axis labels. (4) Vertical viridis colorbar with CJK high/low semantic end-labels.
- **Motion hints**: This is the un-annotated base state of the heatmap; slide 34 adds dashed call-out rectangles on top of the same chart — i.e. 33 → 34 is a staged annotation build.
- **Reusable pattern**: This slide is an instance of "data-viz result + Key-Insight sidebar" using a viridis heatmap (title/insight on the left, chart on the right).
- **One-offs / inconsistencies**: The heatmap's in-chart title on this frame appears doubled/overlapping (jittered text) versus slide 34's clean render — likely a source-PNG rendering artifact rather than a design choice; marked UNCERTAIN. Footer says "Exp 3: Forced Prefixes" but the slide content is a cross-language heatmap (topic/footer mismatch); recorded as observed, not reconciled (footer transcribed verbatim).
