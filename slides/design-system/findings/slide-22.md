# Slide 22 — The Attention Tracker
- **Archetype**: data-viz (grid of small-multiples over a full-bleed field, with a centered text overlay)
- **Grid & layout**: Full-bleed matrix of small distribution-curve thumbnails, roughly 7 columns × 5 rows, tiling the entire canvas edge-to-edge with small uniform gutters (slide 22). No conventional safe-area margin — thumbnails run to the edges. A centered text block floats over the middle rows, occupying the horizontal center and vertically the middle band; the surrounding thumbnails are dimmed relative to it so the overlay reads as foreground (slide 22).
- **Type roles**: head/body only, no mono. One centered CJK title line "調整 K，找到最精準的「內鬼」" (bold white, largest text on slide). Below it a single legend row with two short labels "K=1" and "K=4" (smaller, lighter) where "K" is italic Latin mixed inline with CJK/`=` digits (slide 22). Mixed-script: Latin italic "K" sits inline with fullwidth CJK punctuation 「」and the halfwidth comma is fullwidth "，".
- **Color / tokens**: Near-black background tier. Each thumbnail plots two overlapping filled distribution curves — a purple/violet series and an olive/chartreuse (lime-family) series — the recurring two-color pairing. Lime accent is load-bearing here: it marks the "K=4" legend dot and highlights two specific grid cells with a bright lime dashed border (one upper-right, one lower-center) (slide 22). Empty/placeholder cells use a grey dashed outline with no curve; filled cells use a solid faint panel. White vs grey split: title is white, legend "K=1" dot is white while "K=4" dot is lime.
- **Components present**: (1) Small-multiple thumbnail grid — each cell a mini distribution-overlay chart, some cells rendered as empty grey-dashed placeholders. (2) Highlight state — lime dashed border ring around selected cells (2 highlighted). (3) Centered title overlay. (4) Legend — two swatch-dots (white=K=1, lime=K=4) with labels. No capsules on this slide.
- **Motion hints**: The two lime-highlighted cells vs many dashed-empty cells suggest a build/selection reveal (stepping through K values, cells populating/highlighting as K changes). Legend K=1/K=4 implies a toggle or scrub between states.
- **Reusable pattern**: This slide is an instance of "a full-bleed grid of small-multiple charts with a centered explanatory overlay and a lime-highlighted selection."
- **One-offs / inconsistencies**: The dashed-empty placeholder cells look like an incidental artifact of a partially-populated grid rather than a canonical component. The two lime-highlighted cells are not aligned to any obvious symmetry (one upper-right, one lower-center), so highlight placement appears content-driven, not gridded.
