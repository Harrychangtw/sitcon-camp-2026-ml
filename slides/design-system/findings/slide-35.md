# Slide 35 — Exp 3: Forced Prefixes
- **Archetype**: other(experiment-intro) — same hybrid as slide 32: big left title + 實驗目標/實驗流程 capsule pair top-right + monospace config sidebar bottom-left.
- **Grid & layout**: Left ~40%: large two-line left-aligned title upper-left ("實驗三：" / "強制前綴的注意力劫持"), with a faint monospace config block anchored bottom-left. Right ~55%: two stacked capsules (實驗目標 top, 實驗流程 below/larger). Faint background grid. Footer present (slide 35).
- **Type roles**: head (large bold CJK title; bold CJK capsule headings), body (CJK capsule body + numbered flow items), mono (low-contrast monospace config columns bottom-left). CJK+Latin mix inside capsule/flow text (e.g. "Prompt", "Output Tokens", "Focus Score").
- **Color / tokens**: Near-black background. Title white-vs-grey split: "實驗三：" mid-grey, "強制前綴的注意力劫持" bright white bold. Capsules are dark rounded elevated cards. Bottom-left mono block is very low-contrast dark grey (recessive), matching slide 32. No lime accent; no viridis ramp.
- **Components present**: 2 CAPSULES (vertical stack, right column) + 1 monospace config sidebar — same anatomy family as slide 32.
  - Capsule A — 實驗目標: emoji = 🎯 dart; bold zh heading "實驗目標" (no en subtitle); vertical divider rule; single body paragraph "強迫模型在回答開頭必須包含特定句型時（如強制輸出是或否），格式要求是否會擾亂注意力機制。". Anatomy = emoji + head │ vertical rule │ body.
  - Capsule B — 實驗流程: emoji = 🧬 DNA; bold zh heading "實驗流程" (no en subtitle); HORIZONTAL rule under the heading; then a numbered 1/2/3 CJK list ("設定四種前綴條件：無前綴、無意義字串、布林判斷、推理鏈。", "將相同的惡意指令輸入模型，並利用 Prompt 引導模型以指定前綴開始生成。", "記錄模型在生成前 100 個 Output Tokens 時，每一步對系統安全指令 Focus Score 的動態變化。").
  - Monospace config sidebar (bottom-left, slide 35 mono crop): low-contrast columns — "Target Models" (Qwen/Qwen3-0.6B, -1.7B, -4B, …), "Conditions:" (baseline, garbage_prefix, boolean_judgment, …), "Model Configs:" (max_output_tokens: 100, threshold: 0.50, tempature: 0.1 [sic]).
- **Motion hints**: Two capsules (目標 → 流程) + numbered flow list suggest a top-down staged reveal.
- **Reusable pattern**: This slide is an instance of "experiment-intro" — identical structure to slide 32 (big left title + 實驗目標/實驗流程 capsules + recessive mono config sidebar). This is the canonical experiment-intro layout.
- **One-offs / inconsistencies**: Config text contains "tempature" (misspelling of temperature) — transcribed as-seen. Capsules carry no en subtitle (unlike slide 31). Capsule A vs B differ internally (vertical rule + body vs horizontal rule + numbered list), consistent with slide 32. Footer "Exp 3: Forced Prefixes" genuinely matches this slide's content (unlike 33/34).
