# Slide 16 — The Attention Tracker
- **Archetype**: capsule-list (two grouped capsule stacks) with an adjacent code panel — a hybrid; the hero is the capsule grid so capsule-list is primary.
- **Grid & layout**: Full-bleed dark canvas, faint square grid (slide 16). Left ~quarter holds a monospace prompt block, left-aligned (slide 16). The center-right is two rounded-rectangle CONTAINERS, each holding a vertical stack of 3 capsules; the two containers are separated by a large "…" ellipsis, and a trailing "…" sits to the right of the second container (slide 16). A left→right arrow runs beneath the containers labeled 淺層網路 (shallow) on the left and 深層網路 (deep) on the right — a depth-progression axis (slide 16). Same oversized grey watermark title "Multi-head Attention" bleeds across the top, partly occluded by the containers (slide 16).
- **Type roles**: head = huge grey watermark Latin title (slide 16); the left prompt uses mono (monospace, syntax-tag styling) (slide 16); capsule headings = bold white CJK; capsule subtitles = smaller grey Latin; axis labels = small grey CJK. Title ≫ capsule heading > capsule subtitle. CJK heading paired with Latin subtitle inside each capsule (slide 16).
- **Color / tokens**: Background darkest tier; containers a slightly lighter dark panel with rounded corners (slide 16). Title = grey watermark tier; capsule heading = white tier; subtitle = grey tier (slide 16). No lime text accent here; the accent color lives in the heatmap thumbnails, which use the viridis ramp (dark purple → teal/green → lime/yellow) — the same ramp seen on slides 19/20/21 (slide 16). Left prompt tags (<system>/<user>) read in a muted/desaturated code palette.
- **Components present**:
  - **CAPSULE (slide-16 variant)** — anatomy: NO emoji, NO number. Contains a bold CJK heading (top-left) with a smaller grey Latin subtitle directly beneath, and a small square viridis heatmap thumbnail in the top-right corner (slide 16). NO body line, NO divider rule. Internal padding is generous; capsules sit within a shared rounded container rather than each having their own visible border. Six total (3 + 3): left container = 記憶自省/Identity, 語意擷取/Semantic Focus, 局部語法/Previous Token; right container = 全局錨點/Global Sink, 結構邊界/Tag Matching, 系統守護/System Anchor (slide 16). Placement = two vertical stacks of 3, side by side. A vertical "⋮" ellipsis sits mid-stack in each container implying omitted heads.
  - Monospace prompt block on the left (system/user chat markup) (slide 16).
  - Depth axis arrow with 淺層網路 → 深層網路 labels (slide 16).
  - Top disclaimer footnote (see one-offs).
- **Motion hints**: The left→right depth axis and the ellipses imply a build that reveals layers from shallow to deep (slide 16) — inferred.
- **Reusable pattern**: "Grouped capsule stacks (heading + Latin subtitle + heatmap thumbnail) arranged along a shallow→deep axis" — the layer-progression capsule instance.
- **One-offs / inconsistencies**: Top-center small-grey disclaimer footnote "*為便於概念理解，本視覺化已省略 FFN, MLP, Residual Stream 等機制/結構" is incidental, not canonical (slide 16). This capsule variant (thumbnail, no number, no body line) is distinct from slide 19's numbered capsules — do not merge the two definitions. Aspect ratio here reads wider/16:9-native vs. some sibling slides.
