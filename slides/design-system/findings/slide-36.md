# Slide 36 — Exp 3: Forced Prefixes
- **Archetype**: data-viz
- **Grid & layout**: Two-column split. Left ~65% width holds a large line chart; right ~35% column holds a "Key Insight:" text block. The chart has its own title centered over the plot area. Footer band at the very bottom (left "SITCON 2026", center "36/41", right "Exp 3: Forced Prefixes"). Slide title sits on one line across the top, left-aligned, spanning full width above both columns. Generous top optical margin above the title; chart and text column share the main body band. (slide 36)
- **Type roles**: head (top title, mixed grey+white split — see Color), body (right-column "Key Insight:" label + two numbered paragraphs of CJK body), mono/label (Latin chart title "Qwen3-8B Output Prefix Focus Score Evolution (Unsafe)", axis titles "Focus Score" / "Output Token Length", numeric axis ticks, and the top-right legend labels which are CJK). Right column has a bold "Key Insight:" (Latin) header then two numbered items whose lead phrase (順從帶來的假安全感 / 偵測延遲) appears bolder/emphasized before a CJK body run. Title ~2× body height, heavier weight. CJK body is regular weight, tight line spacing; Latin numerals and axis labels are lighter grey. (slide 36)
- **Color / tokens**: Near-black background (chart plot area is pure/deep black). Title uses the white-vs-grey split: leading phrase "前綴干擾:" in grey, the emphasized clause "安全防線的延遲生效" in white. Body text is light grey on black. Chart is a multi-series line plot with four saturated series colors matching the legend swatches: yellow (無前綴), magenta/pink (布林判斷), cyan (推理鏈), purple/violet (無意義字串). These read as a bright categorical palette, not a viridis ramp. Top-right of chart has two annotation lines with up/down arrows (↑ 高分 / ↓ 低分) in grey. No lime accent visible on this slide. (slide 36)
- **Components present**: (1) Slide title row. (2) Large multi-series line chart with its own centered chart title, labeled X/Y axes with numeric ticks, and a top-anchored legend of four colored dot + CJK-label pairs arranged as a 2×2 cluster. (3) Legend (color dot + label). (4) In-chart arrow annotations (↑ 高分 = 緊盯規則 (安全) / ↓ 低分 = 忘記規則 / 被帶風向 (危險)). (5) Right-side "Key Insight" text block: bold header + numbered list (1., 2.) where each item has a bold CJK lead phrase then a CJK explanatory sentence. No capsules on this slide. (slide 36)
- **Motion hints**: The two numbered insight items could be staged reveals paired with the chart; not determinable from a static image. (slide 36)
- **Reusable pattern**: This slide is an instance of "chart-left / annotated-insight-right data-viz slide" — a full data chart paired with a numbered takeaway column.
- **One-offs / inconsistencies**: The bright categorical line palette (yellow/magenta/cyan/purple) is chart-specific and distinct from the deck's grey+white+lime UI palette. The in-plot arrow-annotation legend (↑/↓ with parenthetical CJK) is a one-off explanatory device. Chart title is in Latin/mono while surrounding titles are CJK. (slide 36)
