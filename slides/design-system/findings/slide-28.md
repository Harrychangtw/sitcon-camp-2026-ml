# Slide 28 — Exp 1: Context Length
- **Archetype**: other(describe) — a hybrid "experiment brief": left title + bottom-left mono config appendix + right-column capsule stack. (The right side alone is a capsule-list; the whole composition is not.)
- **Grid & layout**: Two main columns plus a footer-adjacent fine-print band (slide 28). Left column top: a two-line title "實驗一：" / "萬字長文的稀釋效應", top-left aligned. Left column bottom: a small monospaced configuration block sitting low, above the footer. Right ~50–55%: a vertical stack of two capsule cards (a small target card + a taller process card). Tighter, more information-dense than slides 24/27.
- **Type roles**: head / body / mono — all three appear (the only slide in range with a true mono block). Title bold white CJK, two lines, largest text. Capsule headings bold white CJK with emoji; capsule bodies grey CJK (the process card is a numbered list). The bottom-left config is small monospaced text mixing Latin model IDs, labels, and CJK-adjacent values. Bodies mix CJK with inline Latin/numerals ("Focus Score", "wikipedia-api", "OOM", "token").
- **Color / tokens**: Near-black background; capsules are raised dark-grey rounded panels (same card tier as 24/27). Foreground split: white headings vs grey body/mono. Lime accent absent; color energy is only from the two emoji. No viridis ramp.
- **Components present**: (1) Two-line section title. (2) Two CAPSULE cards, vertical stack of 2: 🎯 實驗目標 — single grey body "測試當惡意指令被藏在長篇大論的不同位置時，Focus Score 是否會因為上下文過長而失效。" (short card); 🧬 實驗流程 — a numbered 4-step grey list (taller card). Capsule anatomy here: emoji + bold white CJK heading in a header row, body below (no English subtitle; no visible internal vertical divider — a lighter variant than 24/27). (3) **Mono config appendix** (bottom-left) — small monospaced fine print laid out in columns: a "Target Models" column listing model IDs (Qwen3-family entries), a "Model Config" column, and an "Articles" column listing article titles (e.g. "Python", "World War II", "Democracy", …). This is the only mono/tabular block in the range.
- **Motion hints**: The numbered 實驗流程 list invites a step-by-step reveal (1→4). Two distinct card sizes (short goal, tall process) suggest they may appear in sequence.
- **Reusable pattern**: This slide is an instance of "experiment-brief layout: title + goal/process capsule pair on the right + a mono run-config appendix in the lower left."
- **One-offs / inconsistencies**: The lower-left mono config block is unique to this slide in the range and reads as reference/reproducibility metadata rather than a canonical design component. The capsules here are simpler than 24/27 (no English subtitle, no clear vertical divider, variable heights), so this is a third, looser capsule treatment — likely content-driven rather than a formal variant.
