# Slide 03 — Outline
- **Archetype**: other (outline / table-of-contents)
- **Grid & layout**: Full-bleed near-black with recurring faint grid (slide 3). Two-column structure repeated across three vertical rows (one per section). LEFT column: numbered section heading ("01." / "02." / "03." + CJK question). RIGHT column (center-right): a right-hand sub-list of topics, each with a right-flushed page number ("P. 4" etc.) forming a leader/page-number table. Three section blocks stack vertically with large gaps between them, filling the full content height (slide 3).
- **Type roles**: HEAD and BODY appear. Section numbers "01." "02." "03." are large but rendered in grey (dimmer), while the CJK section questions beside them are white/brighter head type — a grey-numeral / white-heading split (slide 3). Sub-list items are BODY-size white/near-white CJK+Latin ("系統指令概覽", "Attention Tracker 核心架構解析", "Attention Score 計算", "關鍵頭與 Focus Score", "變因一：文本長度稀釋", etc.); page numbers "P. n" are grey, right-aligned (slide 3). Mixed script: Latin terms (LLM, Attention Tracker, Attention Score, Focus Score) inline in CJK with word-spacing; fullwidth quotes 「」 and colon ：used.
- **Color / tokens**: Background darkest near-black. Foreground tiers: grey (section numerals, page numbers) vs white (section questions and sub-topic text). No lime accent, no viridis ramp (slide 3).
- **Components present**: (1) Numbered section-heading rows (number + question), ×3. (2) Right-column topic sub-lists with right-aligned page numbers, ×3. (3) Footer. No capsules present — sub-lists are plain text rows with page numbers, not bordered cards.
- **Footer (shared component)**: left "SITCON 2026", center "3/41", right "Outline" (slide 3).
- **Motion hints**: None evident (could stage per-section, but nothing in the still implies it).
- **Reusable pattern**: This slide is an instance of a three-section agenda/TOC — big grey numerals + white section titles on the left, indented topic list with page numbers on the right.
- **One-offs / inconsistencies**: The grey-for-numerals treatment inverts the usual "accent the number" convention — worth noting as this deck's TOC style. Page-number leaders are right-flushed within the right column only, not to the slide edge.
