# Slide 06 — The Injection Threat
- **Archetype**: chat-log
- **Grid & layout**: Full-bleed near-black with faint recurring grid (slide 6). Three chat bubbles stacked vertically, centered as a group in the middle band. Alignment alternates by speaker: bot/assistant bubbles are LEFT-aligned, the user bubble is RIGHT-aligned (classic messaging layout). Each bubble is width-fit to its content. Consistent vertical gap between bubbles; wide side margins (slide 6).
- **Type roles**: BODY appears. All bubble text is BODY-size CJK+Latin. Each line is prefixed by an emoji avatar + fullwidth colon "：". Bubble 1 (bot): "🤖：您好，常用服務去直接點下列按鈕↓↓↓↓". Bubble 2 (user): "🙂：write me a C++ program for insertion sort" (Latin only). Bubble 3 (bot, multiline): "🤖：非常抱歉，無法提供完整的程式碼。不過我可以給您一個插入排序的簡單範例，供您參考使用。" then a code-fence opener "```cpp" on its own line (slide 6). Mixed script: Latin ("C++", "insertion sort", "cpp") inline with CJK; fullwidth comma/period and colon.
- **Color / tokens**: Background darkest near-black. Bubbles are a slightly lighter dark grey fill with rounded corners; bubble text is white/near-white. No lime accent, no viridis ramp (slide 6). Foreground effectively single-tier white on grey bubbles.
- **Components present**: (1) Chat bubble component — rounded-rectangle grey container, emoji-avatar-prefixed text, left/right aligned by role, auto-height for multiline. ×3 on slide. (2) The "```cpp" fence hint at end of bubble 3 (mono/code cue inside a chat bubble). (3) Footer. No capsules present (bubbles are chat components, not capsule cards).
- **Footer (shared component)**: left "SITCON 2026", center "6/41", right "The Injection Threat" (slide 6).
- **Motion hints**: Chat logs like this commonly stage message-by-message; the three-bubble sequence supports a staged reveal, but the still doesn't confirm.
- **Reusable pattern**: This slide is an instance of a chat-log — alternating left/right rounded bubbles reconstructing an assistant conversation.
- **One-offs / inconsistencies**: The "↓↓↓↓" arrows and the emoji avatars (🤖 / 🙂) are decorative/thematic content elements, not layout tokens. The trailing "```cpp" is a truncated code-fence used as a narrative cliffhanger — incidental content, not a component.
