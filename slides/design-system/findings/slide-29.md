# Slide 29 — Exp 1: Context Length
- **Archetype**: data-viz
- **Grid & layout**: Two-zone split. Full-width title bar across the top (slide 29). Below, a large line-chart panel occupies roughly the left ~65% of the canvas; a right column (~30%) holds a "Key Insight:" heading and a numbered analysis list (1/2/3). A small right-aligned annotation block sits at the top-right of the title row ("↑ 高分 = 緊盯規則 (安全)" / "↓ 低分 = 忘記規則 / 被帶風向 (危險)"). Content respects generous outer margins; the chart plot area is inset with its own axes.
- **Type roles**: head (CJK title, bold), body (CJK running text in the right column and the top-right legend note), mono/label (Latin axis labels "Focus Score", "Input Token Length (Binned)", numeric ticks, and chart title "Qwen3-8B Context Scaling Analysis (Binned)"). Right-column list uses bold CJK lead phrases ("分離度：", "平滑化的假象。", "固定閾值的失效。") followed by lighter body text. Mixed CJK+Latin in the chart title sits on one baseline.
- **Color / tokens**: Near-black background tier. Title uses the white-vs-grey split — "宏觀趨勢：" rendered in mid-grey, "穩定的防禦邊界" in bright white, both same bold weight (slide 29, title crop). Series colors: 正常 = lime/chartreuse (reads as the system accent lime; state as relationship, not measured hex), 後端注入 = violet/purple, 前端注入 = magenta/pink, 中間注入 = cyan. A dashed horizontal reference line sits near Focus Score ≈ 0.5. Each series has a translucent shaded confidence band behind the line. Right-column numeral markers and "Key Insight:" appear in a light/desaturated tone.
- **Components present**: (1) Full-width slide title. (2) Top-right two-line annotation legend (↑/↓ semantics). (3) Line chart with 4 series + shaded bands + dashed threshold line. (4) In-chart legend capsule: a rounded dark pill containing a 2×2 grid of colored dots each paired with a CJK label (正常/後端注入 left column, 前端注入/中間注入 right column) (slide 29, legend crop). (5) Right-side "Key Insight:" numbered list (3 items). No standalone content capsules on this slide.
- **Motion hints**: Numbered Key Insight list (1→2→3) suggests possible staged reveal; not certain from a static frame.
- **Reusable pattern**: This slide is an instance of "data-viz result + Key-Insight sidebar": a full-width title, a large left chart, and a right-hand numbered takeaways column.
- **One-offs / inconsistencies**: The ↑/↓ semantic annotation top-right is a per-experiment gloss, not a generic component. The in-chart legend pill is chart-local (distinct from the capsule component seen on 31/32). Lime series color appears tied to the system accent but is used here as a data-series color, not as an accent highlight.
