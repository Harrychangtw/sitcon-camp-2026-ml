# Slide 05 — The Injection Threat
- **Archetype**: statement (with an inline lime-accent stat)
- **Grid & layout**: Full-bleed near-black with faint recurring grid (slide 5). Two centered statement lines sit as a block around the vertical middle, left-aligned within a wide centered measure (starts left of center, spans most of the width). A small centered source/citation caption sits at the very top of the slide, above all content. Large empty space above and below the statement (slide 5).
- **Type roles**: HEAD/BODY-scale statement text appears. Two lines, both large-ish body/statement weight (not the biggest head sizes). Line 1: "Claude Code 加上插件，開發如魚得水，但..." (white). Line 2: "36.8% 的 AI 插件中，藏著 Prompt Injection 與惡意程式碼。" (white, except the number). Top caption is small grey text: "🔗 Snyk ToxicSkills Report: AI Agent Security Audit" (slide 5). Mixed script: Latin "Claude Code", "AI", "Prompt Injection" and the "36.8%" figure sit inline in CJK runs with word-spacing; fullwidth comma ，and period 。used.
- **Color / tokens**: Background darkest near-black. Foreground: white statement text vs grey source caption. LIME accent used precisely on the statistic "36.8%" only — the sole colored element, at the same size as surrounding body text (inline emphasis, not enlarged). No viridis ramp (slide 5).
- **Components present**: (1) Top source/citation caption (with 🔗 link glyph). (2) Two-line statement with one lime-highlighted figure. (3) Footer. No capsules present.
- **Footer (shared component)**: left "SITCON 2026", center "5/41", right "The Injection Threat" (slide 5).
- **Motion hints**: Plausible two-step reveal (setup line, then the payoff line/number), but nothing in the still confirms it.
- **Reusable pattern**: This slide is an instance of a statement slide — a setup + punchline sentence with a single lime-accented statistic inline.
- **One-offs / inconsistencies**: Deliberately NOT a big-stat: "36.8%" is set at body size and only recolored lime, not blown up as a hero number — verified against the surrounding text. The top-anchored source caption (with 🔗 glyph) is a distinct small element; note lime is reserved here for the data figure specifically.
