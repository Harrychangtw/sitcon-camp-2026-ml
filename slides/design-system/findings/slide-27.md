# Slide 27 — Exp 1: Context Length
- **Archetype**: capsule-list
- **Grid & layout**: Two-zone split identical to slide 24 (slide 27). Left ~40%: large section title "實驗變因", left-aligned, vertically centered. Right ~50–55%: single column of four evenly-spaced capsule cards filling the vertical safe area, right-aligned to a margin. Comfortable outer margins.
- **Type roles**: head / body, plus a small English subtitle role inside capsules. Title "實驗變因" is the dominant element, bold white CJK. Each capsule has a bold white CJK heading with a smaller grey English subtitle directly beneath it, and a grey body block to the right. Bodies mix CJK with inline Latin/numerals ("Focus Score", "LLM", "0.6B", "8B", "Sure, I can help"). No mono block.
- **Color / tokens**: Near-black background; capsules are raised dark-grey rounded panels (same card tier as slide 24). Foreground split: white headings / grey English subtitles / grey body. Accent color comes almost entirely from the emoji glyphs — lime is essentially absent on this slide. No viridis.
- **Components present**: Four CAPSULE cards, vertical stack (single column of 4). Anatomy per capsule: emoji icon top-left + bold white CJK heading, **with an English subtitle line under the heading** (this is the variant vs slide 24, which has no subtitle); a thin vertical divider rule separates the icon+heading zone from the body; single grey body block on the right; generous uniform internal padding. The four: 🔪 文本長度 / "Context length" — "當惡意指令被藏在長篇大論的開頭、中間或結尾。"; 🧠 模型大小 / "Model Size" — "比較 0.6B 及 8B 模型。模型夠大是否效果就會較好？"; 🌍 多國語言 / "Language" — "Focus Score 是在 LLM 推論時「順便」讀出來的，幾乎不增加延遲。"; 📌 強制前綴 / "Prefixes" — "當模型被迫以 \"Sure, I can help\" 開頭時，注意力機制是否會出現盲點。"
- **Motion hints**: Four-card even stack suggests a top-to-bottom sequential reveal, matching slide 24.
- **Reusable pattern**: This slide is an instance of "big left-aligned section title + right-hand vertical stack of icon/heading/divider/body capsules" (the bilingual-heading variant; shares the layout with slide 24).
- **One-offs / inconsistencies**: **The "多國語言 / Language" capsule body is a copy-paste error in the source deck** — its text "Focus Score 是在 LLM 推論時「順便」讀出來的，幾乎不增加延遲。" is *verbatim identical* to slide 24's "零耗算力" body and has nothing to do with multilingual behavior. Transcribed as-is per reverse-engineering rules, but flagged as almost-certainly leftover placeholder text, not intended copy. Also note: the English subtitle presence distinguishes this capsule variant from slide 24's zh-only capsules.
