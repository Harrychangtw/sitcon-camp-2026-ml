# Slide 14 — The Attention Tracker
- **Archetype**: data-viz
- **Grid & layout**: Two-panel comparison across a full-bleed near-black canvas (slide 14), split by a faint vertical rule down the center. LEFT panel 人類視角 ("human view"): a vertical list of the sentence tokens (The / animal / didn't / cross / the / road / because) on the left with a focus token "it…" to their right, and colored arrows fanning from "it…" back to the tokens. RIGHT panel 機器視角 Attention Matrix ("machine view"): a lower-triangular attention heatmap grid with the same tokens labeled on both the top axis (rotated/vertical) and the left axis. A large faint ghost watermark "Attention Overview" spans the upper area behind both panels. Top-center small header "LLM Visualization" with an icon. A color legend sits top-center-left: 不關注 → 很關注. Footer at bottom.
- **Type roles**: head — panel titles 人類視角 / 機器視角 Attention Matrix (zh + Latin mono-ish), the ghost watermark "Attention Overview" (very large, dim, Latin). Token labels rendered in a mono/typewriter face (Latin). Legend labels zh small. Mixed CJK panel titles + Latin data labels.
- **Color / tokens**: Near-black background. The VIRIDIS ramp is the defining token here and appears in three places: (1) the legend bar 不關注(dark purple)→很關注(lime-yellow); (2) the attention matrix cells, colored by weight along viridis (dark purple = low, teal/green = mid, lime-yellow = high); (3) the LEFT-panel arrows reuse the SAME ramp (bright lime-yellow arrow to the strongly-attended token "animal", dim purple/blue arrows to weakly-attended tokens). Watermark and grid lines are dim grey. This is the only heatmap/viridis slide in the range.
- **Components present**:
  - Legend bar (1): horizontal viridis gradient with end labels 不關注 / 很關注.
  - Human-view panel: token list + focus token "it…" + fan of viridis-colored attention arrows.
  - Machine-view panel: lower-triangular attention matrix (causal-mask triangle — only cells at/below the diagonal are filled), tokens on top axis (vertical/rotated labels) and left axis, cells shaded by viridis weight. Notable bright cells: The→The (diagonal, high), it→animal (high lime), matching the left panel's story.
  - Ghost watermark title "Attention Overview".
  - Top-center "LLM Visualization" running label (also on slide 11).
- **Motion hints**: The paired human/machine views and the highlighted it→animal link suggest a staged reveal (pick a token, light up its attention) — this looks like a still of an interactive/animated attention canvas.
- **Reusable pattern**: This slide is an instance of a side-by-side "human intuition vs attention-matrix" data-viz using a shared viridis weight ramp.
- **One-offs / inconsistencies**: The lower-triangular (causal-mask) matrix shape is intrinsic to attention, not a styling choice. The viridis ramp is exclusive to this slide in the range and is the canonical heatmap token. The oversized dim "Attention Overview" watermark is a device not seen elsewhere here. The "LLM Visualization" top-center tag recurs from slide 11, distinct from the footer-right section label.
