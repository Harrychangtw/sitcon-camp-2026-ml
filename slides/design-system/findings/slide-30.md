# Slide 30 — Exp 1: Context Length
- **Archetype**: data-viz
- **Grid & layout**: Same two-zone layout as slide 29 — full-width title bar; large line-chart panel on the left ~65%; right ~30% column with "Key Insight:" heading and a numbered list (this slide shows items 1 and 2). Same top-right ↑/↓ annotation block. Same generous outer margins and inset plot area (slide 30).
- **Type roles**: head (bold CJK title), body (CJK right-column text with bold lead phrases "初始邊界模糊：", "長度衰減與混淆："), mono/label (Latin chart title "Qwen3-0.6B Context Scaling Analysis (Binned)", axis labels "Focus Score" / "Input Token Length (Binned)", numeric ticks). An in-chart CJK annotation "難以區分" sits above a bracket spanning the right portion of the plot.
- **Color / tokens**: Near-black background. Title white-vs-grey split: "局限揭露：" mid-grey, "小模型的注意力崩塌" bright white, same bold weight (slide 30). Series colors identical to slide 29: lime/chartreuse (正常), violet (後端注入), magenta (前端注入), cyan (中間注入), each with translucent confidence bands. Dashed horizontal reference line near 0.5. A dashed bracket/annotation marks the "難以區分" region on the right side of the chart in a light grey.
- **Components present**: (1) Full-width title. (2) Top-right ↑/↓ annotation legend. (3) Line chart with 4 banded series + dashed threshold. (4) In-chart legend pill (2×2 dot+label grid: 正常/後端注入, 前端注入/中間注入). (5) In-chart "難以區分" span bracket annotation. (6) Right-side "Key Insight:" numbered list (2 items here). X-axis extends further (ticks to ~4000+) vs slide 29's ~1750, reflecting the 0.6B model's range.
- **Motion hints**: The "難以區分" bracket over a late-x region reads as a call-out that could animate in; numbered list may stage.
- **Reusable pattern**: This slide is an instance of "data-viz result + Key-Insight sidebar", the twin of slide 29 for a different model (0.6B vs 8B).
- **One-offs / inconsistencies**: The "難以區分" bracketed span annotation is unique to this slide (not present on 29). This slide's Key Insight list has 2 items vs slide 29's 3 — item count is content-driven, not a fixed grid.
