# Slide 09 — The Injection Threat
- **Archetype**: capsule-list
- **Grid & layout**: Two-zone split on a full-bleed dark canvas (slide 9). LEFT ~third: a very large title 常見防禦 vertically centered, left-aligned, sitting in the left third with a faint square grid texture behind it. RIGHT ~two-thirds: a single vertical stack of 4 evenly-spaced capsules, right-anchored with a right margin. Capsules are full-width within the right zone, equal height, consistent vertical gaps. Footer below.
- **Type roles**: head appears twice over — the oversized left title (white, bold, by far the largest type on the slide, ~3–4× body) and the per-capsule zh heading (white, bold). Each capsule also has an en subtitle (smaller, grey/dim, directly under the zh heading) and one body line (grey, smaller). CJK title + CJK capsule headings with Latin subtitles and Latin terms inline in body (Constitutional AI, Llama Guard, API, Human-in-the-loop).
- **Color / tokens**: Near-black background. Faint grid lines on the left. Foreground title in bright white; capsule zh heading white (bright tier) vs en subtitle grey (dim tier) vs body mid-grey — the white-vs-grey split appears WITHIN each capsule (bright zh over dim en). Capsule surfaces raised dark-grey rounded panels. No lime accent. No viridis.
- **Components present**:
  - Section title block (1): 常見防禦, oversized, left third.
  - Capsules (4, vertical stack, right two-thirds). CANONICAL capsule anatomy: emoji icon (left, legible: 📝 for 提示工程, 🏰 castle for 外部護欄, 🧹 broom for 特徵過濾, 📦 box for 執行沙盒) + **zh heading + en subtitle two-line pair** (提示工程/Prompt Engineering, 外部護欄/External Guardrails, 特徵過濾/Input Scanning, 執行沙盒/Output Sandboxing) + single zh body line. Emoji sits to the left of the heading pair; body sits to the right (a two-column internal layout: icon+title | body). Rounded padding, no visible internal divider rule.
- **Motion hints**: The 4-item stack reads as a sequential build (one capsule at a time), but the still shows no explicit motion cue.
- **Reusable pattern**: This slide is an instance of a "big left title + vertical stack of canonical zh/en capsules" list.
- **One-offs / inconsistencies**: This is the CANONICAL capsule (emoji + zh heading + en subtitle + one body line), the reference against which slide 8's stripped variants (Latin-only or zh-only, no subtitle) read as deviations. Note the en subtitle "Output Sandboxing" pairs with 執行沙盒 (lit. execution sandbox) — a copy mismatch, but transcribed as shown. Internal layout differs from slide 8 (icon-left + body-right here vs icon-by-heading + body-below on slide 8).
